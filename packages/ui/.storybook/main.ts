// Storybook has moved to apps/storybook — run `pnpm --filter @pulsedesk/storybook dev` from the workspace root.
// This file intentionally left empty to prevent accidentally running Storybook from packages/ui.
export default {};
